{
  "name": "agentscape",
  "version": "1.15.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run assets:validate && npm run test && npm run build",
    "assets:validate": "node tools/validate-assets.mjs",
    "agent:gateway": "node scripts/openai-compatible-agent-gateway.mjs",
    "agent:probe": "node scripts/probe-test-agent.mjs"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.17.3",
    "@gltf-transform/core": "^4.4.2",
    "@gltf-transform/functions": "^4.4.2",
    "@recast-navigation/core": "^0.43.1",
    "@recast-navigation/generators": "^0.43.1",
    "three": "^0.179.1",
    "three-mesh-bvh": "^0.9.1"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^6.1.0",
    "vitest": "^3.2.4"
  }
}
